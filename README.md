 What is the difference between var, let, and const?
 
Ans to the Qustion No *01*
এগুলো variable declare করার জন্য ব্যবহার হয়।
var = পুরনো JavaScript, value change করা যায় ।
let = value change করা যায়।
const = value change করা যায় না।

What is the spread operator (...)?

Ans to the Qustion No *02*
Spread operator ব্যবহার করা হয় array বা object copy বা combine করতে ব্যবহার।

What is the difference between map(), filter(), and forEach()?

Ans to the Qustion No *03*
Difference between map(), filter(), and forEach()
এগুলো array method।
map() = নতুন array তৈরি করে ।
filter() = condition অনুযায়ী data নেয় ।
forEach() = শুধু loop করে ।

What is an arrow function? 
Ans to the Qustion No *04*
Arrow function হলো short function।

What are template literals? ans to the qustion at a biginer
Ans to the Qustion No *05*
Template literals ব্যবহার করা হয় string এর মধ্যে variable বসানোর জন্য।
